services:
  - type: web
    name: luisearch-youtube-crawler
    runtime: python
    plan: free
    region: oregon
    buildCommand: pip install -r requirements.txt || true
    startCommand: python3 crawler.py
